namespace enums
{
    // The name chosen is not the best, but basically, this enum contains names for different web applications
    public enum WebApplicationType
    {
        CarApp,
        BankApp,
    }
}